import React from "react";

const TaskForm = () => {
    return (
        <form>
            <label>Name</label>
            <input type="text" placeholder="Task" />
        </form>
    );
};

export default TaskForm;
